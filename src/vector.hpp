#ifndef VECTOR_HPP
#define VECTOR_HPP

#include <vector>
#include <algorithm>    // std::sort
using namespace std; // Ainda não utilizei outra para entender melhor como isso funciona

int pesquisarVector(vector<float> v, float numero);

#endif
